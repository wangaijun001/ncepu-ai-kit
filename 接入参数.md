# 华北电力大学 AI 大模型 —— 接入 Qoder / WorkBuddy 配置参数

> 实测日期：2026-09-24 ｜ 全部 8 项自检通过

## 一、通用参数（两个平台都一样）

| 参数 | 填写值 |
|---|---|
| **Base URL / 接口地址** | `http://202.204.64.234:8080/api` |
| **Model ID / 模型名** | `qwen3.8-27b` |
| **API Key** | 参考网页教学生成（`sk-` 开头，35 位） |
| 上下文窗口 | 256K（`max_model_len = 262144`） |
| 协议类型 | OpenAI 兼容 → **Chat Completions API** |

### 三条铁律

1. **Base URL 末尾不要加斜杠**。加了会变成 `/api//chat/completions`，返回 `405 Method Not Allowed`。
2. **不要加 `/v1`**。`/v1/chat/completions` 和 `/api/v1/chat/completions` 实测均返回 `405`。
   只有 `/api/chat/completions` 这一条路径可用。
3. **模型名是 `qwen3.8-27b`**，不是学校文档里写的 `qwen3-coder:30b`。填错会报模型不存在。

## 二、模型能力（决定配置时怎么勾）

| 能力 | 实测结果 | 配置建议 |
|---|---|---|
| 文本对话 | ✅ 通过 | — |
| 工具调用 function calling | ✅ 通过（正确返回 `get_weather` + `{"city":"北京"}`） | 可开启 Agent / 工具能力 |
| 流式输出 SSE | ✅ 通过 | 正常开启 |
| 视觉 / 图片输入 | ✅ 通过（纯色图 2/2 答对） | **勾选「视觉」** |
| 思考模式 | ✅ 支持，推理内容在 `reasoning` 字段 | 可开启 |
| 关闭思考 | ✅ `chat_template_kwargs={"enable_thinking": false}` 生效 | 「允许关闭思考」可勾 |
| 思考强度 | ⚠️ 仅 `low` / `medium` / `xhigh`（+`none`）可用 | 见下方坑一 |
| 最大输出 | ⚠️ 输入+输出合计 ≤ 262144 | 见下方坑二 |

后端为 **vLLM** 部署的 `Qwen3.8-27B-FP8`（FP8 量化）。

## 三、Qoder 配置步骤

1. 打开 **Qoder 设置** → 左侧导航栏选「**模型**」
2. 点「**+ 添加**」
3. 「供应商」→ 展开「**自定义**」分组 → 选「**OpenAI Compatible**」
4. API 类型选「**Chat Completions API**」
5. 填写：
   - 接口地址（Base URL）：`http://202.204.64.234:8080/api`
   - API Key：`sk-...`（从 `api_key.txt` 复制）
   - Model ID：`qwen3.8-27b`
6. 点「**下一步**」，配置模型能力：
   - 显示名称：随意，例如 `NCEPU-Qwen3.8-27B`
   - 支持的上下文窗口：选 256K 档（若列表无此项，选最接近的较大值）
   - 视觉：**开启**
   - 思考模式：按需开启
7. 点「**校验并添加模型**」，通过后保存
8. 对话框里即可选择该模型使用

> 自定义模型不消耗 Qoder Credits，但 Repo Wiki 仍使用固定模型、会消耗 Credits。

## 四、WorkBuddy 配置步骤

1. 打开 **设置** → 「**模型**」
2. 点「**添加模型**」
3. 接入方式选「**自定义 / Custom**」（不要选预置供应商，也不要用 Ollama 那项）
4. 填写：
   - URL：`http://202.204.64.234:8080/api`
   - API Key：`sk-...`
   - 模型名：`qwen3.8-27b`
5. **高级配置 →「自定义协议」保持关闭**（默认关闭即可）
   - 关闭时 WorkBuddy 会自动补全标准 `/chat/completions` 路径，
     拼接结果正好是 `http://202.204.64.234:8080/api/chat/completions` ✅
   - 若开启，则会原样使用你填的地址，反而会拼错
6. 保存后，在对话界面的模型选择器里选「自定义模型」分组下的这一项

> 配置仅保存在本地 `workbuddy/models.json`，不上传云端。

### 「添加模型」弹窗逐项填写对照

| 界面项 | 填 / 勾 | 依据 |
|---|---|---|
| 模型名称 | `qwen3.8-27b` | 这是发给 API 的 **Model ID**，必须逐字符一致，不是随便起的显示名 |
| 工具调用 | ☑ 勾 | 实测正确返回 `tool_calls`，**不勾则 Agent 能力失效**（读写文件、执行命令都用不了） |
| 图片输入 | ☑ 勾 | 实测纯色图 2/2 答对 |
| 思考模式 | ☑ 勾 | 实测默认返回 `reasoning` 字段 |
| 仅思考模式 | ☐ **不勾** | 实测可关闭思考，不是「只能思考」的模型 |
| 允许关闭思考 | ☑ 勾 | 实测 `enable_thinking=false` 生效，思考可关 |
| 自定义协议 | ☐ **不勾** | 不勾才会自动补 `/chat/completions`；勾了反而拼错 |
| 默认思考强度 | 自动（使用请求层默认值） | 保持默认即可 |
| 支持的思考强度 | 只勾 **低 / 中 / 超高** | ⚠️ 见下方说明，勾「高」或「极致」会在运行时报 400 |
| 输入（上下文） | **128K** | 与输出合计不得超过 262144，见下方说明 |
| 输出（最大输出） | **32K** | 同上 |

### ⚠️ 坑一：思考强度只有三档真的可用

实测 API 对 `reasoning_effort` 的校验结果：

| 传入值 | 结果 |
|---|---|
| `low` | ✅ 接受 |
| `medium` | ✅ 接受 |
| `xhigh` | ✅ 接受（服务端默认值） |
| `none` | ✅ 接受（等于关闭思考） |
| `high` | ❌ 拒绝：`Unexpected reasoning effort high. Supported types are xhigh (default), medium, and low.` |
| `max` / `maximum` / `minimal` / `ultra` / `extreme` | ❌ 拒绝 |

界面上是 低 / 中 / 高 / 超高 / 极致 五档，按字面一一对应的话，
**只有 低、中、超高 三档在服务端真实存在**。

所以：**只勾「低 / 中 / 超高」，把「高」和「极致」留空。**
勾上它们不会立刻报错，但运行时一旦选到这两档，请求会被服务端以 400 拒绝。

### ⚠️ 坑二：输入 + 输出 不能超过 262144

模型总上下文是 262144（256K），这个数字是 **输入 + 输出之和**，不是各自独立。

实测边界：

| 请求 | 结果 |
|---|---|
| `max_tokens=200000` | ✅ 接受 |
| `max_tokens=262144` | ❌ 400：`This model's maximum context length is 262144 tokens. However, you requested 262144 output...` |
| `max_tokens=300000` | ❌ 400：`cannot be greater than max_model_len=max_total_tokens=262144` |

如果输入选 256K、输出选 64K，两者相加 320K 已超上限；
长对话把输入窗口撑满时，请求就会直接被拒。

**建议：输入 128K + 输出 32K**，合计 160K，留足余量。
日常任务 128K 输入完全够用，响应也更快；确实要处理超长文档再考虑调大输入、同时把输出调小。

## 五、注意事项

- **网络可达性**：`202.204.64.234` 属教育网公网段，本机实测可直连、无需 VPN。
  但换到其他网络环境（家里宽带、手机热点、公司网络）不一定能通，用前先跑一次自检脚本确认。
- **模型体量**：27B FP8 量化，属中小体量模型。工具调用、视觉、流式都支持，
  但复杂多步 Agent 任务（长链路规划、大范围重构）的表现会明显弱于主流旗舰模型，产出建议人工复核。
- **数据流向**：对话内容会发送到学校服务器。涉及敏感/涉密内容请勿使用。
- **密钥安全**：`api_key.txt` 里是明文密钥，不要提交到 Git 仓库或分享给他人。

## 六、出问题时怎么排查

先跑自检脚本，它会逐项定位：

```cmd
"E:\ai file\workbudy\ai study\check-ncepu-ai.bat"
```

常见报错对照：

| 现象 | 原因 | 处理 |
|---|---|---|
| `405 Method Not Allowed` | Base URL 带了尾斜杠或 `/v1` | 改成 `http://202.204.64.234:8080/api` |
| `401 Not authenticated` | 密钥错/过期/带多余空格 | 回网页重建密钥，重新写入 `api_key.txt` |
| `模型不存在 / model not found` | 模型名写错 | 改成 `qwen3.8-27b` |
| 连接超时 | 当前网络访问不到该 IP | 换网络环境或确认是否需要校园网 |
