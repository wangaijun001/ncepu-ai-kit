# 华北电力大学 AI 编程大模型 · 使用包

> 一句话：学校提供了一套**免费**的 AI 编程大模型服务（qwen3.8-27b），
> 这个包帮你**在 5 分钟内接进自己的编辑器 / 客户端**。
>
> 打包日期：2026-09-24 ｜ 服务参数均为实测

---

## 先看这个：3 步上手

### 第 1 步 · 注册并拿到自己的 API 密钥

1. 浏览器打开 **http://202.204.64.234:8080** ，注册账号并登录
2. 点击右上角**圆形头像** → **设置** → **账号** → **API 密钥**
3. 点「**创建新安全密钥**」→ **保存**，复制生成的 `sk-` 开头的字符串

> 官方说明原文：https://lab.ncepu.edu.cn/bslc/rjzy/0b468cf674694cf6b9feaf95883a7352.htm
> ⚠️ 该文档发布于 2025-11-21，**有三处内容已过时**，务必以本包文档为准（详见第 3 节）。

### 第 2 步 · 把密钥填进 `api_key.txt`

打开本目录下的 `api_key.txt`，把密钥粘到**第 1 行**，保存。
（文件里那行 `PASTE_YOUR_KEY_HERE` 是占位符，不用删，脚本会自动跳过。）

### 第 3 步 · 双击 `check-ncepu-ai.bat` 自检

会自动检查 8 项能力（连通性、模型列表、对话、工具调用、流式、视觉……），
全部通过就说明你这边能正常用了。

**然后打开 `harness连接指南.html`**，找到你用的工具，照着填参数即可。

---

## 需要装什么

| 用途 | 需要 Python？ |
|---|---|
| 只看文档、按文档手动配置编辑器 | ❌ 不需要 |
| 跑自检脚本 `check-ncepu-ai.bat` | ✅ 需要 Python 3.9+ |
| 跑本地反代 `start-proxy.bat` | ✅ 需要 Python 3.9+ |

Python 下载：https://www.python.org/downloads/
安装时**务必勾选 `Add Python to PATH`**。依赖 `requests` 会在首次运行时自动安装。

---

## 三条铁律（配置时最容易踩的坑）

```
1. Base URL 末尾不要加斜杠   → 加了会 405
2. Base URL 不要加 /v1        → 加了会 405
3. 模型名是 qwen3.8-27b       → 学校文档写的 qwen3-coder:30b 已不存在
```

完整可用地址（只有这一条路径能通）：

```
POST http://202.204.64.234:8080/api/chat/completions
```

**如果某个工具报 405**：说明它会自己往地址后面拼 `/v1`，用本包的反代解决 ↓

```bash
双击 start-proxy.bat
# 然后把工具里的 Base URL 改成：http://127.0.0.1:8788/v1
```

---

## 包内文件说明

| 文件 | 说明 |
|---|---|
| **`使用说明.md`** | 就是本文件 |
| **`harness连接指南.html`** | ⭐ **主文档**，双击用浏览器打开。涵盖 Cline / Roo Code / Continue / Aider / Claude Code / Cherry Studio / Dify 等各类工具的配置，含截图 |
| `harness连接指南.md` | 主文档的 Markdown 版（方便复制文字） |
| `接入参数.md` | Qoder / WorkBuddy 的详细配置步骤 + 坑位说明 |
| `check-ncepu-ai.bat` | 双击运行自检（首次会自动创建并打开 `api_key.txt`） |
| `start-proxy.bat` | 双击启动本地反代，解决报 405 的工具 |
| `test_ncepu_ai.py` | 自检脚本本体（8 项能力检测） |
| `ncepu_proxy.py` | 本地反代本体（把标准 `/v1` 转成学校的 `/api` 路径） |
| `api_key.txt` | 密钥文件，**需要你自己填**（第 1 行） |

---

## 模型能力速查

| 能力 | 支持 |
|---|---|
| 文本对话 / 流式输出 | ✅ |
| 工具调用（Agent 读写文件、执行命令） | ✅ |
| 图片输入（视觉） | ✅ |
| 思考模式（推理过程） | ✅ 内容在 `reasoning` 字段 |
| 上下文 | 256K，但**输入 + 输出合计**不能超过 262144 |

**两个必须知道的限制：**

1. **思考强度只有 3 档真实可用**：`低` / `中` / `超高`。
   界面上如果显示「低/中/高/超高/极致」五档，**只勾前三个里对应的**，
   勾「高」或「极致」运行时会报 `400 Unexpected reasoning effort high`。

2. **输入 + 输出 ≤ 262144**：建议填**输入 128K + 输出 32K**，
   选「256K 输入 + 64K 输出」会超限直接报错。

---

## 常见问题

| 现象 | 原因 | 处理 |
|---|---|---|
| `405 Method Not Allowed` | Base URL 带了尾斜杠，或工具自己拼了 `/v1` | 去掉尾斜杠；或双击 `start-proxy.bat` 后填 `http://127.0.0.1:8788/v1` |
| `401 Not authenticated` | 密钥错 / 过期 / 带了空格 | 回网页重新创建密钥 |
| `model not found` | 模型名写错 | 填 `qwen3.8-27b` |
| `400 Unexpected reasoning effort high` | 思考强度选了「高」 | 只勾 低/中/超高 |
| `400 maximum context length is 262144` | 输入+输出超限 | 输入 128K + 输出 32K |
| 连接超时 | 当前网络到不了学校服务器 | 该地址属教育网公网段，换网络环境再试 |
| 双击 bat 一闪而过 | 没装 Python，或没勾 Add to PATH | 装 Python 后重试 |

---

## 注意事项

- **密钥是私人的**：每人用自己的账号建密钥，不要共用、不要外传。
  本包里的 `api_key.txt` 是空模板，不包含任何人的真实密钥。
- **数据会发往学校服务器**：涉密 / 敏感内容不要输入。
- **共享资源**：这是校内师生共用的服务，别跑大规模批量推理。
- **模型体量**：27B 稠密模型，日常编程辅助够用，
  但复杂多步 Agent 任务（大型重构、长链路规划）产出建议人工复核。

---

## 想自己部署一套？

学校这套是 **vLLM + Open WebUI** 的开源组合，理论上可以自建。
需要 48GB 显存（FP8 权重约 28GB）或两张 24GB 卡，家用显卡建议走 4-bit 量化。
本包未包含部署教程，可参考 vLLM 官方文档与 Open WebUI 文档。
